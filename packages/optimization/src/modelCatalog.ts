// The model registry lives in @agentlab/contracts so the agent runtime and editor share it.
export { MODEL_CATALOG, cheapestModelInTier, estimateCostUsd, getModel } from "@agentlab/contracts";
export type { ModelInfo, ModelTier } from "@agentlab/contracts";
